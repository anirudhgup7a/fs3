// Base class: Person
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  // Method to display basic information
  displayInfo() {
    console.log(`Name: ${this.name}, Age: ${this.age}`);
  }
}

// Subclass: Student (extends Person)
class Student extends Person {
  constructor(name, age, course) {
    super(name, age); // Call parent constructor
    this.course = course;
  }

  // Override method to include course
  displayInfo() {
    console.log(
      `Student Name: ${this.name}, Age: ${this.age}, Course: ${this.course}`
    );
  }
}

// Subclass: Teacher (extends Person)
class Teacher extends Person {
  constructor(name, age, subject) {
    super(name, age); // Call parent constructor
    this.subject = subject;
  }

  // Override method to include subject
  displayInfo() {
    console.log(
      `Teacher Name: ${this.name}, Age: ${this.age}, Subject: ${this.subject}`
    );
  }
}

// Create instances
const student1 = new Student("Alice", 20, "Computer Science");
const teacher1 = new Teacher("Mr. Smith", 40, "Mathematics");

// Call methods
student1.displayInfo();
teacher1.displayInfo();
